# @scomm/pubkey-protocol

Language-neutral SComm pubkey protocol: identity, canonical signed bytes, algorithm registry, error codes, and conformance fixtures.

See `fixtures/` in this package and `pubkey/docs/client-adapter/architecture.md`.
