# @scomm/pubkey

SComm pubkey **adapter** for Outlook (HTTP, enrollment, Vault orchestration). Vault **format** is [CKVF](https://github.com/Cryptographic-Key-Vault-Format/sdk-js).

This package used to live in `scomm-ai/sdk_pubkey`. It is now part of the Office workspace.
